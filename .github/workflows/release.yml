name: release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
env:
  ALPINE_VERSION: 3.23
jobs:
  build-binaries:
    strategy:
      fail-fast: false
      matrix:
        arch: ["i386","amd64","armv7l","arm64"]
        runner:
          - ubuntu-24.04
          - ubuntu-24.04-arm
        exclude:
          - arch: amd64
            runner: ubuntu-24.04-arm
          - arch: i386
            runner: ubuntu-24.04-arm
          - arch: arm64
            runner: ubuntu-24.04
          - arch: armv7l
            runner: ubuntu-24.04
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v6

      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist community/fio
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist community/libqrencode
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist community/redis
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/7zip
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/curl
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/htop
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/iperf3
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/iproute2
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/lsof
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/mariadb
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/nano
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/netcat-openbsd
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/nmap
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/pigz
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/postgresql17
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/procps-ng
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/rsync
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/socat
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/strace
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/tcpdump
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/vim
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist main/wget
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist custom/mysql57
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist custom/mysql80
      - run: ALPINE_VERSION="${ALPINE_VERSION}" BUILDARCH="$( echo ${{ matrix.arch }} | sed 's,arm64,arm64v8,')" docker build --platform="linux/$(echo ${{ matrix.arch }} | sed 's,armv7l,arm/v7,')" -o=dist custom/mysql84

      - run: |
          for file in dist/*; do
            mv "$file" "${file}_${GITHUB_REF_NAME}_linux_${{ matrix.arch }}"
          done

      - uses: actions/upload-artifact@v4
        with:
          name: static-binaries-artifact-${{ matrix.arch }}
          path: dist/*
          retention-days: 1
          if-no-files-found: error

  merge-binaries:
    runs-on: ubuntu-24.04
    needs: build-binaries
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: static-binaries-artifact-*
          path: dist
          merge-multiple: true

      - run: cd dist && sha256sum * > SHA256SUMS

      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
